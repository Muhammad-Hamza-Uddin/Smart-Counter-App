# Smart-Counter-App
The Smart Counter App is a React.js application built using the useState Hook. It allows users to increment, decrement, reset, and modify the counter by ±5 while keeping the value between 0 and 20. The app also displays Even/Odd status, generates a random number, and supports dynamic background color changes through interactive buttons.
